hacer un npm install react react-dom al iniciar para asegurarse de tener todo instalado

instalar una extensión ES7+ React 
usar rafc para generar el react
import React, { useState } from 'react' (obligatorio recordar, osea en tema de ponerlo cuando haga un archivo)
no olvidar hacer 'npm run dev' para cargar la página y abrir http://localhost:3000

importnte revisar si esta instalado el "react-router-dom"
ctrl + s para terminar la ejecución del programa al menos cuando se instala algo.


Usar la primera página creada y convertirla en React (literalmente)
posiblemente pida un crud
usar una base de datos
usar el tema de las secciones para el menú de la página, osea, distintas páginas.


ir a firebase, iniciar sesión, crear un proyecto. (se puede usar desde el celular la base de datos, como es de google uno se ahorra el iniciar sesión, firebase ocupa una base de datos no relacional.)
al crear la base de datos y querer añadirlo al proyecto, uno deberá instalarlo

npm install firebase

al hacer un commit a github NO SUBIR EL ARCHIVO "credenciales".

despues borrar:
crear en Firebase un Credenciales.tsx y pegar dentro esto.
export const firebaseConfig = {
    apiKey: "AIzaSyCqkCQr5NPHDx-IJYgJXNsZ3DP1CDtdPrY",
    authDomain: "iei2023fe-e93d7.firebaseapp.com",
    projectId: "iei2023fe-e93d7",
    storageBucket: "iei2023fe-e93d7.appspot.com",
    messagingSenderId: "272781748180",
    appId: "1:272781748180:web:8cc7785ee2876b2b67d841",
    measurementId: "G-S0MDZ94288"
};

crear una carpeta assets/img y luego ver si puedo agregar imagenes.

copiar, pegar y adaptar.
usar firebase. no usar otra cosa.
subirlo a hosting, osea buscar en google hosting gratuito. poner en el terminal "npx build"


cosas que hacer:
validar el nombre de usuario, apellido, tunear la validación de edad,
ver como validar la edad de la persona para que tenga una fecha de nacimiento y no una fecha erronea.
poner fondos a cada página.
ordenar un poco todo.
finalmente añadir validaciones. en la validación de server que haga que sea distinto a "seleccione un server"

en el video hacer una comparativa en el video de la evalucion 2 y la nueva. (osea, la página. mostrar ambas páginas.)


6 secciones.
Home.
Que es terraria?.
imagenes del juego.
Trailer.
Formulario.
Registros.

hosting permite que la app quede en internet.






para ahcer funcionar el npx next build

debo mover las carpetas dentro de la carpeta componentes a una nueva carpeta dentro de src llamada pages.